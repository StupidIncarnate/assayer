/**
 * PURPOSE: Barrel export for @assayer/shared transformers — the pure renderings BOTH surfaces need,
 *   so the CLI's report and the desktop's panel cannot describe the same case two different ways.
 *
 * USAGE:
 * import { arrangeTextTransformer } from '@assayer/shared/transformers';
 */

// Subpath export entry for @assayer/shared/transformers

export * from './src/transformers/arrange-text/arrange-text-transformer';
