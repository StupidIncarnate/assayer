/**
 * PURPOSE: Barrel export for @assayer/desktop brokers — the desktop launch seam the CLI spawns.
 *
 * USAGE:
 * import { desktopLaunchBroker } from '@assayer/desktop/brokers';
 */

// Subpath export entry for @assayer/desktop/brokers

export * from './src/brokers/desktop/launch/desktop-launch-broker';
